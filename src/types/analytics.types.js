// ─── Analytics Types ─────────────────────────────────────────────────────────

// No imports needed for JavaScript (remove type imports)

export const AnalyticsOverview = {} // Just for reference - interfaces don't exist in JS

// These are type definitions that don't exist in JavaScript
// In JavaScript, you would just use regular objects without type checking

// The actual JavaScript code would just be comments since interfaces are TypeScript-only

/*
export interface AnalyticsOverview {
  dateRange: DateRange
  totalSent: number
  totalDelivered: number
  avgDeliveryRate: number
  avgOpenRate: number
  avgCtr: number
  totalUnsubscribes: number
  totalHardBounces: number
  activeCampaigns: number
  newContacts: number
  emailSent: number
  whatsappSent: number
  emailAvgOpenRate: number
  whatsappAvgReadRate: number
}

export interface TrendDataPoint {
  date: string
  sent: number
  opens: number
  clicks: number
}

export interface ChannelBreakdown {
  channel: CampaignChannel
  sent: number
  deliveryRate: number
  openRate: number
  ctr: number
}

export interface CampaignPerformanceRow {
  campaignId: string
  campaignName: string
  channel: CampaignChannel
  sent: number
  delivered: number
  deliveryRate: number
  openRate: number
  ctr: number
  bounceRate: number
  unsubscribeRate: number
  sentAt: string
}

export interface AudienceEngagementRow {
  contactId: string
  fullName: string
  email: string | null
  engagementScore: number
  totalCampaignsReceived: number
  totalOpens: number
  totalClicks: number
  lastEngagedAt: string | null
}

export interface AnalyticsFilters {
  dateFrom: string
  dateTo: string
  channel?: CampaignChannel
  listId?: string
}
*/